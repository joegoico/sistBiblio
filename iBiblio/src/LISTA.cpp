#include "LISTA.h"

template <typename T>LISTA<T>::LISTA()
{
    //ctor
}
template <typename T>
void LISTA<T>::agregarLibro(T libro){
    /*a medida que va leyendo el archivo, va
      agregando de manera ordenada por anio
      de menor a mayor*/
};

template <typename T>
void LISTA<T>::listarLibrosPorRango(int r_inferior, int r_superior){
    /*lee la lista hasta que encuentra el limite inferior
      del rango, entonces empieza a imprimir*/
}
template <typename T>LISTA<T>::~LISTA()
{
    //dtor
}
